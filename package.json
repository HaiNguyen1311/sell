{
  "name": "banhang",
  "version": "1.0.0",
  "description": "",
  "main": "app.js",
  "scripts": {
    "start": "start json-server --watch listProduct.json --port 3000 & start json-server --watch listUser.json --port 3001",
    "startApiProduct":"json-server --watch listProduct.json",
    "startApiUser":"json-server --watch listUser.json",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "json-server": "^0.17.3"
  }
}
